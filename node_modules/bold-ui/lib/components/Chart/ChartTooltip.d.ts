import React from 'react';
import { AxisDomain, TooltipRenderer } from './model';
export interface ChartTooltipProps<XDomain> {
    children: React.ReactElement;
    showTooltip: boolean;
    seriesName: string;
    label: XDomain;
    value: number;
    labelDomain?: AxisDomain;
    renderer?: TooltipRenderer<XDomain>;
}
export declare function ChartTooltip<XDomain>(props: ChartTooltipProps<XDomain>): JSX.Element;
export declare namespace ChartTooltip {
    var defaultProps: ChartTooltipProps<any>;
}
