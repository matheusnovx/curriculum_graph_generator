import React from 'react';
import { Theme } from '../../styles';
import { ComboboxMultiselectComponents } from './ComboboxMenuComponents';
import { DefaultComboboxItemType } from './Combobox';
import { ComboboxSingleselectProps } from './ComboboxSingleselect';
export interface ComboboxMultiselectProps<T> extends Omit<ComboboxSingleselectProps<T>, 'value' | 'onChange' | 'components' | 'multiple' | 'createNewItem'> {
    value?: T[];
    onChange?: (newValue: T[]) => void;
    itemIsEqual(a: T, b: T): boolean;
    components?: Partial<ComboboxMultiselectComponents<T>>;
    clearFilterOnSelect?: boolean;
}
export declare function ComboboxMultiselect<T = DefaultComboboxItemType>(props: ComboboxMultiselectProps<T>): JSX.Element;
export declare const createStyles: (theme: Theme, { disabled, clearable }: ComboboxMultiselectProps<any>, hasSelectedItems: boolean) => {
    wrapper: React.CSSProperties;
    disabled: React.CSSProperties;
    invalid: React.CSSProperties;
    input: React.CSSProperties;
    menu: React.CSSProperties;
};
