export declare type AsyncComboboxLoadFn<T> = (filter: string) => Promise<T[]> | T[];
export declare function useComboboxItemsLoader<T>(items: AsyncComboboxLoadFn<T>, debounceMs?: number): {
    items: T[];
    loading: boolean;
    loadItems: (filter: string) => void;
};
