import { LocaleConfiguration } from '../../../i18n/LocaleContext';
export declare enum AggregatorEnum {
    PERCENTAGE = "PERCENTAGE",
    COUNT = "COUNT",
    AVERAGE = "AVERAGE",
    MAXIMUM = "MAXIMUM",
    MINIMUM = "MINIMUM"
}
export declare type AggregatorLabels = Pick<LocaleConfiguration['aggregators'], 'count' | 'percentage' | 'average' | 'maximum' | 'minimum'>;
export declare type AggregatorFunction = (values: number[], total?: number) => number;
export declare type Aggregator = {
    id: AggregatorEnum;
    label: string;
    value: AggregatorFunction | undefined;
    keyDependent: boolean;
    chain?: Aggregator[];
    suffix?: string;
};
